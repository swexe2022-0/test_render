<% if flash[:info] %>
  <%= flash[:info] %>
<% end %>

<% @products.each do |e| %>
  <p>
    <%= e.name %>
    <%= e.price %>
    <%= link_to '削除', product_path(e), method: :delete %>
  </p>
<% end %>

<p> 
  <%= link_to '商品を新規登録する', new_product_path %>
</p>
